package com.example.docstatus.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Определяет кастомную цветовую палитру для приложения.
 *
 * Этот объект содержит все специфичные цвета, используемые в UI приложения,
 * сгруппированные по их функциональному назначению (например, фон, основной акцент, индикаторы статусов).
 */
object AppColors {
    /** Основной цвет фона приложения, глубокий темный оттенок. */
    val Background = Color(0xFF10161B)

    /** Слегка осветленный цвет для поверхностей, таких как поля ввода. */
    val Surface = Color(0xFF1C242D)

    /** Основной акцентный цвет, используемый для главных действий, например, кнопок. */
    val PrimaryBlue = Color(0xFF2196F3)

    /** Основной цвет для текстового контента. */
    val TextWhite = Color(0xFFEEEEEE)

    /** Цвет для текста-подсказки в полях ввода. */
    val TextHint = Color(0xFF788CA0)

    /** Цвет по умолчанию для иконок. */
    val IconColor = Color(0xFF2196F3)

    /** Цвет рамки для кнопки биометрической аутентификации. */
    val BiometricBorder = Color(0xFF2A3744)

    /** Цвет фона для кнопки биометрической аутентификации. */
    val BiometricBackground = Color(0xFF161D24)

    /** Цвет фона для круглого контейнера с логотипом. */
    val LogoBackground = Color(0xFF162838)

    /** Цвет, представляющий валидный или успешный статус (зеленый). */
    val StatusGreen = Color(0xFF4CAF50)

    /** Цвет, представляющий статус предупреждения или истекающего срока (желтый). */
    val StatusYellow = Color(0xFFFFC107)

    /** Цвет, представляющий невалидный или ошибочный статус (красный). */
    val StatusRed = Color(0xFFF44336)
}
